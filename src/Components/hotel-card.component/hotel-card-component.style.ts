import {StyleSheet} from "react-native";
import {BORDER_RADIUS_SM, GRAY_LIGHT_XX} from "../../../native-base-theme/variables/config";

export default StyleSheet.create({
  image:{
    height: 150,
    width: undefined,
    borderColor: GRAY_LIGHT_XX,
    borderWidth: 1,
    borderRadius: BORDER_RADIUS_SM,
  },

});
